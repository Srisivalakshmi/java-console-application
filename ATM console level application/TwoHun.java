package ListofNotes;

import Notes.Notes;

public class TwoHun extends Notes {

    // Constructor to initialize the note value and count for ₹200 notes.
    public TwoHun(int note, int count){
        // Constructor to initialize the note value and count for ₹200 notes.
        super(note, count);
    }
}
public class TwoHun extends Notes{

    // Constructor to initialize the note value and count for ₹200 notes.
    public TwoHun(int note, int count){
        // Constructor to initialize the note value and count for ₹200 notes.
        super(note, count);
    }
}
