package ListofNotes;

import Notes.Notes;

public class FiveHun extends Notes {

    // Constructor to initialize the note value and count for ₹500 notes.
    public FiveHun(int note, int count){
        // Constructor to initialize the note value and count for ₹2000 notes.
        super(note, count);
    }
}
